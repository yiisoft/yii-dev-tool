<?php

/**
 * This is just an example by which configuration file `slack.local.php` should be created.
 */
return [
    /**
     * Token generated by Slack for Travis CI integration.
     *
     * You can find it in Slack in the settings of "Travis CI" application:
     * 1. Add application "Travis CI" to Slack.
     * 2. Go to the "Settings" tab.
     * 3. Create a new configuration.
     * 4. Find field "Token" in the configuration and copy the value.
     */
    'token' => '',
];
