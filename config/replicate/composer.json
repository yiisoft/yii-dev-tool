{
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "php": "^7.4|^8.0"
    },
    "require-dev": {
        "roave/infection-static-analysis-plugin": "^1.3",
        "phpunit/phpunit": "^9.4",
        "vimeo/psalm": "^4.1"
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "test": [
            "phpunit --colors --no-interaction"
        ]
    }
}
